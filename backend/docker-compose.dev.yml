services:
  db:
    image: postgres    
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: backend
    volumes:
      - pgdata:/var/lib/postgresql
    ports:
      - 5432:5432

volumes:
  pgdata: